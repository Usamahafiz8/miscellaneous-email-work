// Intentionally renders nothing — see app/hiring/layout.tsx. HiringView reads
// the selected id straight from the URL via usePathname().
export default function HiringEmailPage() {
  return null;
}
